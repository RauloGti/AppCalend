import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuapp.calendAap',
  appName: 'CalendAap',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
