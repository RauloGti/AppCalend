import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.CalendAap.tuApp',
  appName: 'CalendAap',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
